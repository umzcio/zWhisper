import {
  Mail,
  MessageCircle,
  StickyNote,
  Users,
  Mic,
  PenLine,
  Sparkles,
} from "lucide-react";
import type { LucideIcon } from "lucide-react";
import type { Mode } from "@/lib/modes";

export const MODE_ICONS: Record<Mode["icon"], LucideIcon> = {
  Mail,
  MessageCircle,
  StickyNote,
  Users,
  Mic,
  PenLine,
  Sparkles,
};
